{
  "name": "app-marketplace-mock",
  "version": "0.1.0",
  "private": true,
  "description": "Mock API única para integrações de marketplace no Netlify",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required for Netlify Functions'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "netlify-cli": "^17.37.1",
    "typescript": "^5.6.2",
    "@types/node": "^20.16.5"
  },
  "engines": {
    "node": ">=20"
  }
}
